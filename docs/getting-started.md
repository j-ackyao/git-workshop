### 1. Generating files

Run `python src/main.py` and confirm files have been generated in `build_errors.txt`.

Note: make sure these files aren't pushed! They contain confidential information!

### 2. Validate error

Make sure that there are no outputs in `build/errors/`!
